import { Button } from "react-bootstrap";

import { useObject } from "react-firebase-hooks/database";

export default function Add({ user, firestore }) {
   return (
    <div>
      <p>Add needs to be updated for Firestore</p>
    </div>
  );
}
